// T-shirt: write a function called make_ shirt() that accepts a size and the text of a message that should be printed on the shirt. the function should be printed on the shirt. the function should print a sentence sumarizing the size of the shirt and the message printed on it.

// call the function.

function make_shirt(size:string, text:string){

    console.log(`creating a ${size} shirt with the message: ${text}`);
}
 
make_shirt ('large','Hello world');

make_shirt ('medium','Hello world');

make_shirt ('small','Hello world');